<?xml version="1.0" encoding="utf-8"?>
<!--
   Backup rules file for Kleos Education app.
   Excludes sensitive user data (tokens, auth info) from Android Auto Backup.
   See https://developer.android.com/guide/topics/data/autobackup
-->
<full-backup-content>
    <!-- Exclude SharedPreferences containing sensitive authentication data -->
    <exclude domain="sharedpref" path="kleos_session_prefs.xml"/>
    
    <!-- Include other SharedPreferences if needed -->
    <!-- <include domain="sharedpref" path="other_prefs.xml"/> -->
</full-backup-content>