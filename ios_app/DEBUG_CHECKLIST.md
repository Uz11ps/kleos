# Чеклист для диагностики проблемы с загрузкой данных

## Что проверить в консоли Xcode:

1. **При запуске приложения должны появиться:**
   ```
   🏠 HomeView appeared, loading data...
   🔄 Loading news...
   🌐 Fetching news from: https://api.kleos-study.ru/news
   ```

2. **Если запрос выполняется, должны появиться:**
   ```
   📤 Request URL: https://api.kleos-study.ru/news
   📤 Request method: GET
   📤 Request headers: [...]
   ```

3. **Если сервер отвечает, должны появиться:**
   ```
   🔍 News response (200), size: XXX bytes
   🔍 Response headers: [...]
   🔍 Response body (first 500 chars): [...]
   ```

4. **Если декодирование успешно:**
   ```
   ✅ Successfully decoded X news items
   📊 JSON structure: Array with X items
   📋 First item keys: id, title, content, ...
   ```

5. **Если есть ошибка декодирования:**
   ```
   ❌ Decode error: ...
   📦 Raw JSON (first 1000 chars): ...
   ```

## Возможные проблемы:

### Проблема 1: Запросы не выполняются
- **Симптом:** Нет логов `🌐 Fetching news from:`
- **Решение:** Проверьте, что методы `loadContent()` вызываются

### Проблема 2: Ошибки сети
- **Симптом:** Логи `❌ Network error:` или `❌ HTTP Error: XXX`
- **Решение:** Проверьте подключение к интернету и доступность сервера

### Проблема 3: Ошибки декодирования
- **Симптом:** Логи `❌ Decode error:` с описанием проблемы
- **Решение:** Проверьте соответствие моделей формату данных сервера

### Проблема 4: Пустые массивы
- **Симптом:** `✅ Successfully decoded 0 news items`
- **Решение:** На сервере нет данных или они неактивны

## Быстрая проверка сервера:

Выполните в терминале:
```bash
curl "https://api.kleos-study.ru/news"
curl "https://api.kleos-study.ru/universities"
curl "https://api.kleos-study.ru/gallery"
curl "https://api.kleos-study.ru/partners"
```

Если команды возвращают данные - сервер работает правильно.

