# Инструкция по тестированию приложения на телефоне

## 📱 Android

### Способ 1: Через Android Studio (рекомендуется)

1. **Откройте проект в Android Studio**
   ```bash
   cd /Users/uz1ps/Downloads/kleos-main
   # Откройте Android Studio и выберите папку app
   ```

2. **Включите режим разработчика на телефоне:**
   - Откройте **Настройки** → **О телефоне**
   - Нажмите 7 раз на **Номер сборки**
   - Появится сообщение "Вы стали разработчиком!"

3. **Включите отладку по USB:**
   - Откройте **Настройки** → **Для разработчиков**
   - Включите **Отладка по USB**

4. **Подключите телефон:**
   - Подключите телефон к Mac через USB кабель
   - На телефоне появится запрос "Разрешить отладку по USB?" → нажмите **Разрешить**
   - Поставьте галочку "Всегда разрешать с этого компьютера"

5. **Запустите приложение:**
   - В Android Studio выберите ваше устройство в списке устройств (вверху)
   - Нажмите кнопку **Run** (▶️) или `Shift + F10`
   - Приложение установится и запустится на телефоне

### Способ 2: Создание APK файла

1. **Соберите APK:**
   ```bash
   cd /Users/uz1ps/Downloads/kleos-main
   ./gradlew assembleDebug
   ```

2. **Найдите APK файл:**
   - Файл будет в: `app/build/outputs/apk/debug/app-debug.apk`

3. **Установите APK на телефон:**
   - **Вариант А:** Перекиньте файл на телефон и откройте его
     - Включите "Установка из неизвестных источников" в настройках
   - **Вариант Б:** Установите через ADB:
     ```bash
     adb install app/build/outputs/apk/debug/app-debug.apk
     ```

### Способ 3: Через WiFi (без USB)

1. **Подключите телефон по USB один раз** и выполните:
   ```bash
   adb tcpip 5555
   adb connect IP_АДРЕС_ТЕЛЕФОНА:5555
   ```
   (IP адрес можно найти в Настройки → О телефоне → Статус)

2. **Отключите USB** и запускайте приложение через Android Studio

---

## 🍎 iOS

### Способ 1: Через Xcode (рекомендуется)

1. **Откройте проект в Xcode:**
   ```bash
   cd /Users/uz1ps/Downloads/kleos-main/ios_app
   open Kleos.xcodeproj
   ```
   Или откройте Xcode и выберите файл `Kleos.xcodeproj`

2. **Подключите iPhone:**
   - Подключите iPhone к Mac через USB кабель
   - Разблокируйте iPhone

3. **Настройте подписи:**
   - В Xcode выберите проект **Kleos** в навигаторе слева
   - Выберите таргет **Kleos**
   - Перейдите на вкладку **Signing & Capabilities**
   - Выберите вашу **Team** (ваш Apple ID)
   - Xcode автоматически создаст профиль разработчика

4. **Выберите устройство:**
   - Вверху Xcode выберите ваш iPhone из списка устройств

5. **Запустите приложение:**
   - Нажмите кнопку **Run** (▶️) или `Cmd + R`
   - На iPhone появится запрос "Доверять этому компьютеру?" → нажмите **Доверить**
   - Приложение установится и запустится

### Способ 2: Через TestFlight (для бета-тестирования)

1. **Создайте архив:**
   - В Xcode: **Product** → **Archive**
   - После создания архива откроется окно **Organizer**

2. **Загрузите в App Store Connect:**
   - Нажмите **Distribute App**
   - Выберите **App Store Connect**
   - Следуйте инструкциям

3. **Добавьте тестировщиков в TestFlight:**
   - Зайдите на [appstoreconnect.apple.com](https://appstoreconnect.apple.com)
   - Добавьте тестировщиков по email

### Способ 3: Через Ad Hoc установку

1. **Добавьте UDID устройства:**
   - В Xcode: **Window** → **Devices and Simulators**
   - Выберите ваше устройство
   - Скопируйте **Identifier** (UDID)

2. **Создайте Ad Hoc профиль:**
   - На [developer.apple.com](https://developer.apple.com) создайте Ad Hoc профиль
   - Добавьте UDID вашего устройства

3. **Соберите и установите:**
   - В Xcode выберите Ad Hoc профиль
   - Создайте архив и установите на устройство

---

## 🔧 Полезные команды

### Android

```bash
# Проверить подключенные устройства
adb devices

# Установить APK
adb install app/build/outputs/apk/debug/app-debug.apk

# Переустановить приложение
adb install -r app/build/outputs/apk/debug/app-debug.apk

# Просмотр логов
adb logcat | grep kleos

# Очистить данные приложения
adb shell pm clear com.kleos.education
```

### iOS

```bash
# Просмотр подключенных устройств
xcrun xctrace list devices

# Просмотр логов устройства
xcrun simctl spawn booted log stream --predicate 'processImagePath contains "Kleos"'
```

---

## ⚠️ Решение проблем

### Android

**Проблема:** Устройство не определяется
- **Решение:** Установите драйверы для вашего телефона (обычно автоматически через Android Studio)

**Проблема:** "Installation failed"
- **Решение:** Убедитесь, что включена "Отладка по USB" и разрешена установка из неизвестных источников

**Проблема:** Приложение не запускается
- **Решение:** Проверьте логи: `adb logcat | grep -i error`

### iOS

**Проблема:** "No signing certificate found"
- **Решение:** Зайдите в Xcode → Preferences → Accounts → добавьте ваш Apple ID

**Проблема:** "Untrusted Developer"
- **Решение:** На iPhone: Настройки → Основные → Управление устройством → Доверьте разработчику

**Проблема:** Приложение не устанавливается
- **Решение:** Проверьте, что Bundle Identifier уникален и соответствует вашему профилю

---

## 📝 Проверка работы API

После установки приложения проверьте:

1. **Авторизация:**
   - Регистрация нового пользователя
   - Вход в систему
   - Верификация email

2. **Основные функции:**
   - Просмотр новостей
   - Просмотр программ и университетов
   - Отправка заявки на поступление
   - Чат с поддержкой

3. **API подключение:**
   - Убедитесь, что приложение подключается к `https://api.kleos-study.ru`
   - Проверьте логи на наличие ошибок сети

---

## 🚀 Быстрый старт

**Для Android:**
```bash
cd /Users/uz1ps/Downloads/kleos-main
./gradlew installDebug
```

**Для iOS:**
1. Откройте `ios_app/Kleos.xcodeproj` в Xcode
2. Выберите ваше устройство
3. Нажмите Run (▶️)

